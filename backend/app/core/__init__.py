"""app.core —— 基础设施最小公共形状（W0b · 卡2 · 第①步）。

边界（docs/06 §12.3 卡2）：
- 本包是 `graph/` 与 `tools/`（以及 `domain/`）唯一允许 import 的基础设施层（docs/03 §3.1）；
- 最小公共形状 = 配置读取 · 统一错误体构造 · 日志入口（只记 ID 与动作，R-O）；
- W0 阶段**仅用标准库**，不引入 fastapi/pydantic（依赖清单归模块一，docs/03 §8.4 第 1 步）。
"""
