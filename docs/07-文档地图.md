# 07 · 文档地图

> | | |
> |---|---|
> | 性质 | **"哪份文档管什么"的唯一权威**。文档清单 · **未产出清单** · 旧仓对照 · 主题索引 · 冲突裁决顺序 |
> | 回答什么 | **想找某一类信息该看哪一篇 · 哪些东西还没产出 · 旧仓那些文档去哪了 · 两处记载打架时以谁为准** |
> | **不**回答什么 | 需求 → [`01-需求分析`](01-需求分析.md) · 数据/链路/失败模式 → [`02-系统设计`](02-系统设计.md) · 分层与目录 → [`03-架构总览`](03-架构总览.md) · 契约清单与覆盖 → [`04-契约层说明`](04-契约层说明.md) · 谁负责 → [`05-分工与协作规范`](05-分工与协作规范.md) · 何时做 → [`06-开发计划与阶段门`](06-开发计划与阶段门.md) · 字段形状 → [`contracts/`](../contracts/) |
> | 上游输入 | `docs/01 §8` **承诺 11**（一主题一篇，**由本文守**）· `docs/01` 附录 C（字段级冲突以契约为准）· `docs/02 §1.6` 的 **R-P2**（未产出文档须登记在本文）· `docs/03 §11`（**主题级**权威映射，本文不复制）· 五份 ADR 头部"本仓对应物见 `docs/07`"的指向 |
> | 下游产出 | `scripts/verify.sh` 的 **R-P2** 检查（未产出目标登记表）· 新人上手的第一个入口 → [`README`](../README.md) |
> | 快照 | ProbCrew v2 · 阶段 A4 |

**旧仓把这件事写成两篇**（`18-文档地图` 与 `00-快速开始` 的一部分），而两篇都没能阻止老问题：旧仓**分工写了 3 篇、计划写了 3 篇、地图写了 2 篇，而系统设计一篇都没有** —— "冗的是重复，缺的是核心"。v2 合成一篇（`ADR-0003` 决策 6），并把这条教训写成了 **`docs/01 §8` 的承诺 11**：

> **一主题一篇文档** | 旧仓分工写了 3 篇、计划写了 3 篇、地图写了 2 篇，而**系统设计一篇都没有** | 谁守：**`docs/07` 单一权威登记**

★ **本文就是那条承诺的执行载体**：每份文档在 §1 登记它的**唯一职责**，每个主题在 §4 指出**唯一权威**；**新增一份文档必须先过 §7 的流程**。

---

## 1. 本仓文档清单

| # | 文档 | 回答什么（**唯一职责**） | 主要读者 | 状态 |
|---|---|---|---|---|
| — | [`README`](../README.md) | **这是什么项目 · 从哪读起 · 怎么跑起来** | 第一次打开仓库的人 | ✅ 已产出（阶段 A4） |
| — | [`第一轮分工`](第一轮分工.md) | ★ **第一轮（批次 1 · W0a–W0d）的完整任务与"应当呈现的形态"** —— **投影单据**：内容全部来自 `docs/06 §11.1` 与 §12，**冲突以 `docs/06` 为准**，**不含任何新口径** | **第一轮的四个领活人** | ✅ 已产出 |
| **01** | [`01-需求分析`](01-需求分析.md) | **要做什么、为什么、什么算做成了** —— 需求清单 · 角色 · 成功标准 · 承诺 · 代价明账 · 术语表 | 全体 | ✅ 已产出（阶段 A） |
| **02** | [`02-系统设计`](02-系统设计.md) | **系统由什么构成、数据长什么样、一次请求怎么走完、出错怎么办** —— 领域/数据模型 · 三条主链路 · 状态机 · 错误语义 · 失败模式 · 规则总表 | 后端 · 前端 | ✅ 已产出（阶段 A2） |
| **03** | [`03-架构总览`](03-架构总览.md) | **代码放在哪、谁可以依赖谁、新增一样东西要动哪几个文件** —— 目录骨架 · 依赖方向 · 扩展点 · 运行形态 | 全体 | ✅ 已产出（阶段 A3） |
| **04** | [`04-契约层说明`](04-契约层说明.md) | **有哪些契约、每份管什么、怎么写、怎么改、机器怎么校验** | 前后端接口双方 | ✅ 已产出（阶段 A3） |
| **05** | [`05-分工与协作规范`](05-分工与协作规范.md) | **谁负责哪一块、写作用域互斥、一人一页、资产由谁产出、找谁批** | 全体 | ✅ 已产出（阶段 A4） |
| **06** | [`06-开发计划与阶段门`](06-开发计划与阶段门.md) | **分几个阶段、每个阶段门的出口条件、28 条 MUST 在哪一步验收、运维与资产何时练/产出** | 全体 | ✅ 已产出（阶段 A4） |
| **07** | **本文** | **哪份文档管什么 · 未产出清单 · 旧仓对照 · 冲突裁决顺序** | 全体 · 新人 | ✅ 已产出（阶段 A4） |
| — | [`contracts/`](../contracts/)（**14 份 schema**） | **字段长什么样**（唯一真相）—— 本文与 `docs/04` 都不复制任何字段 | 前后端 · 门禁 | ✅ 已产出（阶段 A3） |
| — | [`docs/adr/`](adr/) | **为什么这么定，以及推翻了什么** —— 决策的历史记录 | 全体 | ✅ 已产出（0001–0005） |

**两条读法**：

- **从上往下读**（新人）：`README` → `docs/01` §1–§4（要做什么）→ `docs/03`（代码放哪）→ `docs/05`（我负责哪块）→ 领到页面后再读 `docs/02 §7` 与该页的契约。
- **从问题倒查**（改代码时）：先看 **§4 的主题索引**，不要按文件名猜。

---

## 2. ★ 未产出清单（`docs/02 §1.6` 的 **R-P2** 的落点）

**R-P2 原文**：*"链接目标若属**未产出**文档，须在 `docs/07` 的'未产出清单'登记"*。

★ **本节把范围从"文档"扩到"一切被链接的未产出目标"**，理由：本套文档里链接得最多、最容易悬空的目标**并不是文档** —— `scripts/verify.sh`（门禁）· `scripts/eval_verify.py`（评测运行器）· `.github/workflows/ci.yml`（CI）· `content/MANIFEST.json` · 五个 `content/*.json`，全都**被正文引用**，而它们**一份都还不存在**。只登记文档会让 R-P2 查完仍然满地悬空。

### 2.1 文档

**本仓的 7 篇主文档 + 5 份 ADR + `contracts/` 全部已产出** —— **本节当前为空**（这正是阶段 A 的出口之一：阶段门 A 不再有"计划内悬空"的文档链接）。

> ★ **旧仓的 `docs/NN` 引用不算悬空**：`ADR-0001` / `ADR-0002` / `ADR-0003` 头部的说明已写清"文中出现的 `docs/01`、`docs/13`、`docs/20`… **指向旧仓**"。**它们的本仓对应物见本文 §3。**

### 2.2 非文档产物（被正文引用，但尚未产出）

| 产物 | 被谁引用 | 谁产出（`docs/05`） | 何时（`docs/06`） | 现在 |
|---|---|---|---|---|
| `requirements.txt` · `requirements-dev.txt` | `docs/03 §8.4` 第 1 步 · `docs/06 §4` 入口条件 2 · `.github/workflows/ci.yml` | 架构与集成 | **阶段 B 的 W0 骨架包** | ✅ **已产出（2026-09-20 · W0a 第 ① 步）** —— ★ **钉精确版本**（运行时 6 个：`fastapi` · `uvicorn` · `langgraph` · `sympy` · `pydantic` · `bcrypt`；dev 2 个：`pytest` · `jsonschema`）。★ 实测：Python **3.14.7** 的 venv 里两个文件都 `pip install` 装通（依赖闭包 49 个包）。★ `docs/03 §8.4` 的"三个待定问题"（pin / LangGraph / jsonschema 归谁）**已在该节写明答案** |
| `scripts/verify.sh` | `docs/01` 承诺 8/9 · `docs/02 §1.6`（R-A/R-B/**R-P**）· `docs/03 §8.2` · `docs/04 §7` · `docs/06 §8` | 架构与集成 | **阶段 B**（`docs/06 §4` 第 3、10 项） | 🟡 **v0 已产出（2026-09-20 · W0a 第 ⑥ 步）** —— 范围 = **R-P1/P2/P3 + `docs/04 §7` 校验①–⑤**（写死；⑥⑦ 与 R-A…R-Q 全量属 W5）。★ **实测**：`bash scripts/verify.sh` **10/10 通过、退出码 0**；**反向演示**：改坏 `error.schema.json` 的一个 `examples` ⇒ 立刻变红（退出码 1），复原后回到绿。★ 由此 CI 的预检（`ready=true`）**开始真正执行门禁** —— 那个 required check 从此**不再是空跑** |
| `.github/workflows/ci.yml` | `docs/06 §4` 第 10 项 · `docs/03 §8.2`（"接远端后即 CI 入口"） | 架构与集成 | **阶段 B 生效**（文件已在 A4 产出，其步骤**指向 `scripts/verify.sh`**） | 🟡 **已产出（占位接线）** —— ★ **已实跑两次**（运行 #1 `febe8cf` · 运行 #2 **`35331064897`** / `55653a6`，2026-09-18；见 `docs/05 §7.2`）。★ **运行 #1 打出 3 条注解**（逐条查过）：① **warning `门禁未运行`**（预检的，**与预期一致**）· ② warning **`actions/checkout@v4` 以 Node 20 为目标而 runner 已强制 Node 24（弃用）** · ③ notice **`ubuntu-latest` 将于 2026-10-19 起迁移到 Ubuntu 26**（我们只依赖 `setup-python` 钉的 3.11，**不受影响**）。★ **运行 #2 验证了升级**：注解 **3 → 2**（**Node 20 那条消失了**），`Run actions/checkout@v7` 步骤 **`success`** ⇒ ★ **`checkout@v7` 已验证**；★★ **但 `setup-python@v7` 仍未被执行过**（它在预检为假时**始终 `skipped`**）⇒ **它的升级只验证了一半**，真正被跑到要等 `scripts/verify.sh` 产出后的第一次推送（W0a）。★ 运行 #2 的结论同样是 `success` 而四个实质步骤 `skipped` —— 见 `docs/05 §7.2` 的「门禁绿到底证明了什么」 |
| `scripts/backup.py` · `scripts/load_test.py` | `docs/06 §6`（运维演练 ②③④）· 需同步回 `docs/03 §8.2` | 架构与集成 | **阶段 B**（`docs/06 §4` 第 9 项） | ❌ 未产出 |
| `scripts/eval_verify.py` | `docs/03 §8.2` · `docs/06 §5` 第 3 项 | 编排与工具 | **阶段 C 前**（正式集盲测要用） | ❌ 未产出 |
| `scripts/fetch_corpus.py` | `docs/02 §1.5③`(B) · `docs/03 §8.2`（唯一离线出网口） | 编排与工具 | **阶段 B**（F10 的 L1 缓存） | ❌ 未产出 |
| `scripts/manage_users.py` | `docs/03 §8.2` · F15（"接口不可创建管理员"） | 后端与数据 | **阶段 B**（第一个管理员账号） | ❌ 未产出 |
| `content/MANIFEST.json` + 五个 `content/*.json` | … | 见 `docs/05 §2.2`（三个域分担） | **阶段门 B 入口**（`docs/06 §7`） | 🟡 **部分产出（2026-09-21 · W0d）** —— 已产出：`content/problems.json`（181 题）· `content/types.json`（102 题型），主责域"前端页面与内容资产"；**未产出**：`content/mistakes.json` · `content/dag.json`（后端与数据）· `content/formula.json`（编排与工具）· `content/MANIFEST.json`（架构与集成，须五类齐备后才能派生 `content_rev`） |
| `backend/` 与 `frontend/` 的**实现代码** | 全套文档 | 见 `docs/05 §3` | **阶段 B** | 🟡 **骨架就绪（2026-09-20 · W0a）** —— 已落地：`backend/app/main.py`（同源托管 + 单 worker 启动检测）· `frontend/index.html` 与 `core/` 六模块 · `frontend/styles/` 四层 · `frontend/components/` · `backend/tests/` 入口（`conftest.py`）· `requirements*.txt` · `scripts/verify.sh` v0（R-P1/P2/P3 + 契约校验①–⑤）。★ 仍未产出：业务端点（`api/`）· `frontend/features/*` · `identity/` · `learning/` —— 它们属 W1–W4 |
| ★ **远端仓库**（`https://github.com/sqbsayori/ProbCrew-v2`） | `README` 的"门禁与 CI"节 · `docs/05 §7.2`（首次托管五步的第 1、2 步） | 架构与集成 | ★ **已落地（2026-09-18）** —— ★ **已推送两次**（首次 `febe8cf` 07:53 UTC 触发运行 #1；**现为 `55653a6`** 触发运行 #2） | ✅ **已产出**（2026-09-18 创建 · **public** · 默认分支 **`main`** · 创建时为空仓 · ★ **远端 `refs/heads/main` = 本地 `HEAD`**（2026-09-18 实测 `git log origin/main..HEAD` 为空）· **除 `main` 外无其他分支**、`forks_count = 0`（实测）· **无 `LICENSE`**（**有意**：保留所有权利，见下方"公开可见性的三条后果"）· **Issues / Wiki / Projects 开关默认开启而本仓不使用**，`docs/03 §2` 规则 4 · ★ **git 传输用 SSH 写法**（本机 2026-09-18 已换成 SSH）—— ★ 理由：`github.com` 的 **HTTPS 在本机间歇性不通**（同日测到连接超时 37.9 s / 挂起 >60 s / **3 次 15 s 超时**；中间**有过一次** 200，★ **别用一次成功去否定一组失败**），而 **SSH 每次都认证成功**；实测与诊断见 `docs/05 §7.2` 的"传输方式"） |
| ★ **`main` 的分支保护规则** | `README` 的"门禁与 CI"节 · `docs/05 §7.2`（首次托管五步的第 3 步 = 参数的**唯一权威**） | 架构与集成 | ★ **阶段 B 开工前**（四人要共享历史、要能走 PR；**没有保护规则，`docs/05 §7.2` 的"`main` 只收门禁绿的提交"就只是一句话**） | ✅ **已生效（2026-09-18 实测）** —— 走的是 **ruleset** 这一路：`mainprotect`（id 23650124）**`enforcement = active`** ⇒ 匿名读 `…/rules/branches/main` **返回 4 条规则**（含 `required_status_checks`，指向那一串 check 名）· `…/branches` 的 `protected` 也变成 `true`。★ **判据**：`https://api.github.com/repos/sqbsayori/ProbCrew-v2/rules/branches/main` → **非空 = 有规则生效**（★ 这条**两套机制都覆盖**）。★★ **残留待清**：第一个 ruleset `main protect`（id 23650056）**仍在、仍 `disabled`、`include` 为空列表** —— 它**不拦任何东西**，但**别把它改成 Active**（匹配不到任何分支），**建议删掉**。★ **参数不写在这里** —— 唯一权威是 `docs/05 §7.2` 首次托管的第 3 步；★ 同一张表的**第 4 步**（邀请其余三人为 collaborator）**仍未满足**：判据是"**三人各自能推一个分支**"，而远端**仍只有 `main` 一个 ref**（实测）—— ★ **所以"一人一分支"目前仍只在负责人本机成立** |
| ★ **立项书**（或它的认领项清单） | `docs/01 §4` 的登记（2026-09-17 补）· `ADR-0002` 两处提到它 | —（**外部产出**） | ★ **不在本仓，须负责人决定是否纳入** | ❌ 未产出（本仓无此文件，故**无法核对** 28 条 MUST 与它的差异） |
| ★ **中期检查报告 + 阶段性支撑材料** | —（**学校要收的实物**） | — | ★ **学校节点**（本仓不写日期） | ❌ 未产出 |
| ★ **结题材料**（结题报告 / 代表成果 / 答辩材料） | — | — | ★ **学校节点** | ❌ 未产出。★ **代表成果形式未登记**（`docs/06 §13`） |

> ★ **为什么现在要写这张表**：`docs/06 §1.2` 规定"**门只增不改**"，而**阶段门 B 会引用这些还不存在的脚本**。把它们登记在这里，R-P2 才能区分两种红：**"计划内未产出"**（本表）与**"真正的悬空引用"**（表外）。**没有这张表，两者长得一模一样。**

> ★ **公开可见性的三条后果（2026-09-18 —— 远端经 API 实测为 `public`）**：
> **① 盲测隔离的代价上升**：`docs/02 §9` 的"**冻结正式集（注入集 20 道 + 冻结验证集 + 期望判据表）不入主仓**"这条口径**不变**，但 public 之后"误提交"的后果从"**实现人能看到**"升级为"**任何人都能看到，而且历史与 fork 里删不干净**"。⇒ 该口径的**执行面**随之加固：冻结集的落点在 `.gitignore` 里改为 **deny-by-default**（`docs/02 §9` 已同步登记）。
> **② ★ 与 `docs/06 §13.1` 第 1 条（结题成果形式）耦合 —— 此处只登记风险，不改那一条的口径**：若成果形式落在**专利**，源码公开属于**公开披露**，可能使方案**丧失新颖性**（《专利法》第二十四条那六个月的例外**不含**"自己在 GitHub 公开"）；落在**软著 / 实物 / 竞赛**则不受此限。⇒ 2026-09-18 选择"**保持 public**"，等于**把"成果形式 = 专利"这一选项的成本提高了** —— 这句话必须留在这里，否则将来真去申请专利时，**没人知道仓库已经公开过**。
> **③ CI 额度**：**public 仓的 Actions 不计分钟** —— ★ **实测过一次**：2026-09-18 那一次推送触发的 CI 运行**确实没有计费**（public 仓）。若将来转 private，CI 会开始消耗额度（`ci.yml` 一次运行是分钟量级）。★ **可见性是可改的设置，不是常量** —— 转 private 的那一天，这一条与上面那一行"现在"列里的 `public` **要一起改**。

### 2.3 待人工确认的引用（ADR 正文里 R-P2 无法解析的那些）

**本节登记一切"R-P2 判不了、但人读得出目标"的引用**（★ 原口径只收"指向 v2 文档的裸 `§N`"，2026-09-17 放宽 —— 因为**具名引用与旧仓前缀同样进不了门禁**，只登记裸 `§N` 会把它们漏在视野外）。按 `ADR` 纪律（**ADR 正文不改**），本轮**只登记、不改写**，由 R-P2 报出"待人工确认"，**不静默通过**：

| ADR | 位置 | 写法 | 实际指向 | 建议 |
|---|---|---|---|---|
| `0004-改为用户自持凭据与联网资源` | 正文第 15 行 | 裸 `§6` | **`docs/01 §6`**（"可用性验收"） | 若接受改 ADR 正文，加前缀即可；否则保持豁免 |
| `0004-改为用户自持凭据与联网资源` | 正文第 63 行 | 裸 `§6` | 同上 | 同上 |
| `0004-改为用户自持凭据与联网资源` | 正文第 95 行 | 裸 `§6` | **`docs/01 §6`**（同一行自己写着 `docs/01`：… §6 可用性拆两条）★ **2026-09-17 补登记**（此前漏）。★ **登记理由**：**严格读法**下它和上面两处同型（该行没有"前缀紧邻 `§`"的引用，只有行首的 `` `docs/01` `` 领起一个列表）；**宽读法**可以靠"并列省略"救回 —— 而**两种读法给出不同结论这件事本身就是缺陷**，故登记 | 同上 |
| `0005-数据走向与出网面` | 正文第 17 行 | 裸 `§6.3` | **`docs/02 §6.3`**（`code` 语义表） | 同上 |
| `0005-数据走向与出网面` | 正文第 17 行 | 裸 `§8` | **`docs/02 §8`**（失败模式） | 同上 |
| `0003-重开仓库的决策` | 正文第 **59** 行 | ★ **具名 `§风险`** | **本 ADR 的 `## 后果` 节**（该节含"代价与代价控制"表）—— ★ **全仓唯一一处真悬空的引用**：它既不是 `§N`（**R-P2 只解析 `§` + 数字，永远查不到它**），也不指向别篇，所以按本篇判也命不中 | ★ **2026-09-17 补登记**。建议不改 ADR 正文，而是**把 R-P2 的解析范围从 `§N` 扩到 `§` + 任意标识**（`docs/02 §1.6` ⑤） |
| `0003-重开仓库的决策` | 正文第 **83** 行 | `旧仓 docs/13 §1.1` | **旧仓**（不是 v2 的引用） | ★ **已豁免**：该 ADR 头部于 2026-09-17 补上"引用指向旧仓"的声明（★ 原表把行号写成 81，2026-09-17 改正 —— 同表另外四行的行号是准的） |

> ★ **为什么要登记而不是改掉**：这些（除旧仓那一处）在**只有人读**时毫无歧义，但**在 `R-P2` 下不可解析或解析到错的地方** —— 不登记，阶段门 A 的 R-P2 就会把它们报成红灯，而下一轮的人会花时间判断"这是真悬空还是写法问题"。**登记之后，两者的区别是明确的。**
> ★ **本表不是"允许写裸引用"**：`docs/02 §1.6` 的 R-P2 规则不变 —— **指别篇时必须写全前缀**。本表只是把这 **7 处已知例外**挂出来，防止它们被当成新问题。★ **2026-09-17 同时修掉了另外 6 处"能解析但解析错"的引用**（`docs/01` ×3 · `docs/03` ×1 · `ADR-0001`/`0002` 头部 ×2，见 `docs/02 §1.6` ⑥）—— 那 6 处**不在本表登记**：它们的修法是**改写法**，不是"保持豁免"。


---

## 3. 旧仓 24 篇 → v2 对照（`ADR-0001` / `ADR-0002` / `ADR-0003` 指向本节）

**为什么需要这张表**：五份 ADR 是**从旧仓搬来的决策记录**，正文里的 `docs/NN` 引用**全部指向旧仓**（各 ADR 头部已声明）。不给出对照，"本仓对应物见 `docs/07`"这句话就是空的。

| 旧仓文档 | v2 的对应物 | 处置 |
|---|---|---|
| `00-快速开始` | [`README`](../README.md) + [`03-架构总览`](03-架构总览.md) **§8.4（开发与运行步骤）** + 本文 §1 的两条读法 | **重写**（v2 的入口是 README + 一份**步骤**；★ §8.4 于 2026-09-17 补，此前这一行是"声明了、正文没落"） |
| `01-架构总览` | [`03-架构总览`](03-架构总览.md) | **重写**；旧仓 §8"诚实清单"的 10 条逐条处置见 `docs/03 §9.2` |
| `02-事件契约` | [`contracts/events.schema.json`](../contracts/events.schema.json)（**C4**） | **重写**（`docs/04 §2.1`：旧仓同名文件**仅作参考**，不是搬运件 —— `ADR-0003` 决策 1 的搬走清单里只有 `user.schema.json`） |
| `03-动画接入规范` | 无（F12 是 **MAY · 未排期**） | **不继承**（`docs/03 §9.2` 第 3/4 条：`markov-chain` 的 rAF 泄漏与 `bayes` 纯 DOM 动画随 F12 一并后置） |
| `04-分工与协作规范` | [`05-分工与协作规范`](05-分工与协作规范.md) | **合成**（与 `19-成员上手与分工` 合为一篇） |
| `05-开发计划` | [`06-开发计划与阶段门`](06-开发计划与阶段门.md) | **合成**（与 `12` · `17` 合为一篇） |
| `06-页面助手接入规范` | 无 | **不继承**（注入式探针后置 —— `ADR-0003` 决策 2；`page-context` 契约**已删除**） |
| `07-部署与分发` | [`03-架构总览`](03-架构总览.md) §8（部署**视图** + **§8.4 运行步骤**）· `docs/06 §6`（运维演练） | **拆分重写**（"何时练"移到计划篇；★ **"怎么起来"这一半此前没有落点**，§8.4 于 2026-09-17 补） |
| `08-B线方案_学习者建模与教师端` | `docs/01 §9`（不做）· `docs/01` F15（教师角色收窄为**管理页**） | **不继承**（学习者建模属 `docs/01 §9` 明确不做；教师端不建报表） |
| `09-主线范围与数据需求清单` | [`01-需求分析`](01-需求分析.md) §4 + 附录 A | **重写** |
| `10-会议决议落地` | `docs/02 §11.1`（**已裁定事项表**）+ [`docs/adr/`](adr/) | **拆入**（v2 不设"会议记录"文档 —— 决议要么进 ADR，要么进 `docs/02 §11.1`） |
| `11-骨架剪枝清单` | [`03-架构总览`](03-架构总览.md) §2 + §10 | **重写**（旧仓该清单已裁定"**不执行**"） |
| `12-基础开发计划` | [`06-开发计划与阶段门`](06-开发计划与阶段门.md) | **合成** |
| `13-数据分级与部署准备` | [`02-系统设计`](02-系统设计.md) §1.3–§1.5（数据分级 · 存放分工 · 出网面） | **重写**；★ 分级口径**已二次修正**（"外传"从并列一级降为表内一列，见 `ADR-0003` §v2 增量第 7 行） |
| `14-开发交接单` | [`05-分工与协作规范`](05-分工与协作规范.md) §2–§4 | **不继承为文档**（交接靠责任人表 + 一主题一提交，不靠一张单子） |
| `15-口径对照与偏差说明` | `docs/02 §10`（与 A1 的对照）· **本文 §5**（裁决顺序） | **拆入**（"三份口径不同的计划"是旧仓的病；v2 用承诺 11 + §5 治） |
| `16-检索升级对比` | `docs/03 §9.3-1` · `docs/03 §10` 第 9 行 | **不继承**（决策 1A：检索**退化为纯词法**，不用嵌入 / 重排权重） |
| `17-模块化任务书` | [`06-开发计划与阶段门`](06-开发计划与阶段门.md) — **其 §11 工作包（可分派单元）** | **合成**（★ 旧仓该篇的"**可直接分配**"是它的主要用途；v2 的对应物是 **§11 的六个工作包**，2026-09-17 补 —— 此前只收到"阶段与门"，**那一层其实没有**） |
| `18-文档地图` | **本文** | **重写**（旧仓的"每个主题只有一个权威"是本文 §4 + `docs/03 §11` 的两级形式） |
| `19-成员上手与分工` | [`05-分工与协作规范`](05-分工与协作规范.md)（**分工**）+ [`03-架构总览`](03-架构总览.md) **§8.4**（**上手**） | **合成**（★ 拆成两半：分工归 `docs/05`，上手归 §8.4 —— 2026-09-17 补，此前"上手"那一半没有落点） |
| `20-产品形态定义` | [`02-系统设计`](02-系统设计.md) §1.5（形态矩阵） | **重写**；★ **双形态不继承**（`docs/03 §10` 第 1/2 行） |
| `21-主站设计规格` | `docs/02 §7`（前端系统设计）· [`03-架构总览`](03-架构总览.md) §5（模块图与页面注册） | **拆分重写** |
| `22-需求分析` | [`01-需求分析`](01-需求分析.md) | **重写** |
| `23-前端技术栈口径` | [`ADR-0001`](adr/0001-前端采用免构建ESM.md) + `docs/03 §5` | **升格为 ADR**（免构建 ESM 是决策，不是规范） |

**统计**：重写 **9** · 合成 **5** · 拆分重写 2 · 拆入 2 · 升格为 ADR 1 · **不继承 4**（`03-动画接入规范` · `06-页面助手接入规范` · `08-B线方案_学习者建模与教师端` · `16-检索升级对比`）· 不继承为文档 1（`14-开发交接单`）—— 合计 **24**。
> ★ **2026-09-17 改正（R-P1 类）**：原写"重写 11 · 合成 6 · 不继承 2"，**分项之和 25 ≠ 自称合计 24**。三处错法各自不同：**"重写 11"是按子串数的**（`拆分重写` 字面含"重写"，2 条被重复计入）· **"合成 6"是纯笔误**（表体 5 条）· **"不继承 2"只列了括号里的两个名字**，漏了 `08`（第 102 行）与 `16`（第 110 行）。**正确分项相加才等于 24** —— 这正是 `docs/06 §3` 第 9 项要挡的形态，而它此前**不在任何检查的视野内**。

> ★ **旧仓把 9 篇标成"档案层（已归档，不再更新）"**：`05` · `06` · `10` · `11` · `14` 等。**v2 不采用"档案层"这个机制** —— 归档件与现行件混在一处，正是 `15-口径对照与偏差说明` 要解决的病。**v2 的分法是**：现行 → 7 篇正文；历史决策 → [`docs/adr/`](adr/)（**正文不改，增量追加**）。

---

## 4. 主题 → 文档索引（★ **小节级**权威见 `docs/03 §11`）

**本文只做"文档级"路由**：想找某一类信息，去**哪一篇**。

| 想找什么 | 去哪一篇 |
|---|---|
| 这个功能为什么要做 / 达标线是多少 | [`01-需求分析`](01-需求分析.md) |
| 数据表长什么样 / 一次请求怎么走完 / 出错返回什么 `code` | [`02-系统设计`](02-系统设计.md) |
| 目录骨架 / 依赖方向 / 新增节点或页面要动哪几个文件 | [`03-架构总览`](03-架构总览.md) |
| 有哪些契约 / 怎么改契约 / 机器校验什么 | [`04-契约层说明`](04-契约层说明.md) |
| 字段级定义（类型 / 枚举 / 必填） | [`contracts/`](../contracts/)（**唯一真相**） |
| 谁负责哪一块 / 我能改哪些目录 | [`05-分工与协作规范`](05-分工与协作规范.md) |
| 现在到哪一步了 / 什么算过门 | [`06-开发计划与阶段门`](06-开发计划与阶段门.md) |
| 为什么当初这么定 / 推翻了什么 | [`docs/adr/`](adr/) |
| **某个小节该放哪一篇**（最容易放错的几处） | ★ [`03-架构总览`](03-架构总览.md) **§11**（**唯一权威，本文不复制**） |
| 哪些东西还没产出 | **本文 §2** |

> ★ **两级索引是有意的**：本文回答"**去哪一篇**"，`docs/03 §11` 回答"**这一段该放哪一节**"（并给出"别处只能怎样"的约束）。**两级都做会变成两处记载** —— 所以本文**只列到篇**，并把最容易放错位置的那几处的裁决权完整留给 `docs/03 §11`。

---

## 5. 冲突裁决顺序（★ 两处记载打架时以谁为准）

**先按"唯一权威"判，不按"哪边更新"判。** 逐级如下：

| 优先级 | 冲突的是 | 以谁为准 | 依据 |
|---|---|---|---|
| 1 | **字段名 / 枚举取值 / 必填性** | **[`contracts/`](../contracts/)** | `docs/01` 附录 C 原文："凡涉及字段、枚举、取值的字段级定义，一律以 `contracts/` 为唯一真相，**冲突以契约为准**" |
| 2 | **已裁定事项** | `docs/02 §11.1`（**已裁定事项表**） | 同一问题若已有裁定，正文里与之相反的旧措辞**按漏改处理** |
| 3 | 需求与达标线 | [`01-需求分析`](01-需求分析.md) | — |
| 4 | 数据模型 / 链路 / 错误语义 / 规则内容 | [`02-系统设计`](02-系统设计.md) | — |
| 5 | 分层 / 目录骨架 / 依赖方向 | [`03-架构总览`](03-架构总览.md) §2 · §3 | 该篇自己写明"冲突时以本节为准（ADR 是历史记录）" |
| 6 | 契约清单 / 覆盖关系 / 书写与变更约定 | [`04-契约层说明`](04-契约层说明.md) | — |
| 7 | 责任人 / 写作用域 | [`05-分工与协作规范`](05-分工与协作规范.md) | — |
| 8 | 阶段与门 | [`06-开发计划与阶段门`](06-开发计划与阶段门.md) | — |
| 9 | 某段该放哪一篇 / 哪一节 | [`03-架构总览`](03-架构总览.md) §11 | — |
| 10 | **ADR 与正文** | **正文**（ADR 是**历史记录**） | 既有口径：`ADR-0003` 正文不改，差值以 `§v2 增量` 节记录 |
| — | **文档与代码** | **文档** | 代码实现与文档不符时，**要么改代码，要么走变更流程改文档** —— 不许"以实际代码为准"默认掉 |

> ★ **第 2 条要特别记住**：`docs/02 §11.1` 是**唯一一处"裁定结果"的登记表**。本轮之前发生的三处失同步（path `24→25` · `code` `14→16` · MUST `23→28`）**都不是"不知道"**，而是**正文没跟表体走** —— 所以 **R-P1**（计数由表体导出）被列为门禁项。

---

## 6. 本文与 `docs/03 §11` 的分工（防两处记载）

| 问题 | 唯一权威 |
|---|---|
| **有哪些文档 · 每个文档的职责与状态 · 未产出清单 · 旧仓对照 · 冲突裁决顺序** | **本文** |
| **某个主题该放哪一篇的哪一节 · "别处只能怎样"** | **`docs/03 §11`** |

★ **本文不复制 `docs/03 §11` 的表格**。两者的关系是**索引与被索引**：本文说"去 `docs/03 §11` 查小节级归属"，`docs/03 §11` 说"主题 X 的唯一权威是 `docs/02 §3.2`"。**任何一条"主题 → 小节"的归属只在 `docs/03 §11` 出现一次。** ★ 各篇自己的"**不回答什么**"边界声明（`docs/05 §8` · `docs/06 §10`）**不是主题级归属登记** —— 它们写的是"这一篇把某主题**让给**谁"，与 `docs/03 §11` 的"某主题**由**谁负责"是同一事实的两面；**重复的是指针，不是归属表**（2026-09-17 说明）。

---

## 7. 新增一份文档的流程（承诺 11 的执行形式）

**默认答案是"不新增"** —— 先问：**能不能进现有 7 篇的某一节？** 只有当它**回答一个现有 7 篇都不回答的问题**时，才新增。流程：

| 步 | 做什么 | 判据 |
|---|---|---|
| 1 | 先试**并入**：在 `docs/03 §11` 里找到它该属的主题 | 找得到 ⇒ **并入那一篇**，不进第 2 步 |
| 2 | 找不到 ⇒ 走 **ADR** 说明"为什么这需要一篇新文档" | 必须有理由，**不接受"方便查看"** |
| 3 | 通过后：在本文 §1 登记（职责 / 读者 / 状态）+ 在 `docs/03 §11` 登记它负责的主题 | 两处都登记才算落地 |
| 4 | 若它取代了某处的记载：**删掉旧处，留一行指针** | `docs/02 §2.4` / `§7.2` 移入 `docs/03` 就是这么做的（`docs/03 §11` 迁移记录） |

> ★ **本仓有一份登记在案的"例外"**：`docs/第一轮分工.md` 是**投影单据**（`docs/06 §11.1` 与 §12 的**呈现层**，用于发给领活人），**由负责人直接指示产出**，**没有走第 2 步的 ADR** —— 理由是它**不含任何新口径**，因此**不构成"第 8 篇正文"**，登记方式与 `README` 相同（§1 的"—"行）。★ **规则不变**：**凡含新口径的新文档仍须走第 2 步的 ADR**；投影与入口类文件（`README` · `contracts/` · 这份单据）**只登记、不占正文编号**。

> ★ **第 4 步最容易被跳过，也最贵**：旧仓的"三份计划"就是这么长出来的 —— 新版写好了，旧版**没删**，于是"同一件事有两处记载"，下一个人按旧的那份干活。**留指针不留正文**，是这套文档唯一防住这件事的手段。
