# 第二轮分工 · 完整任务与应呈现的形态

> ★ **本文是"投影单据"，不是正文** —— 内容**全部来自** [`06-开发计划与阶段门`](06-开发计划与阶段门.md) 的 **§11（工作包 W1–W4）** 与 **§11.2（三个批次）**、**§4（阶段门 B 的 12 项出口条件）**、**§7（内容资产下界）**、**§9（五条口径）**、**§14（第二轮任务卡：怎么算做完 / 按什么顺序 / 交给谁）**，以及它们指向的 `docs/05` · `docs/03` · `docs/02` · `docs/01 §6`。★ **冲突时一律以 `docs/06` 为准**；★ **本文不新增任何口径、不新增任何工作**。
> **用途**：发给第二轮的四个领活人。★ **本文一律用代号 `人一`–`人四`**（与 [`第一轮分工`](第一轮分工.md) 一致）—— **真实姓名不入仓**（本仓是 public；`docs/06 §12` 明令"不写人名"，`docs/05 §2` 也有"责任人落在**域**而不是**人**"的明账）。★ **代号 ↔ 姓名**的对应由小组**在仓外**维护。
> **范围**：**只有第二轮 = `docs/06 §11.2` 的批次 2 = 工作包 W1–W4**（四条纵切）。★ **批次 3（W5）与阶段门 C 不在本文范围内**（见 `docs/06 §11` · §11.2 · §5）。
> ★ **为什么现在才有本文**：`docs/06 §12` 当时写明"**批次 2 的四条纵切（W1–W4）不在本轮** —— 它们的细节取决于**骨架落地后的实际形状**"。骨架（`#5`）与四张卡（`#3` · `#4` · `#7` · `#8`）已落地 ⇒ 那个前提现在已经成立。

---

## 一、这一轮要做成什么

| 项 | 内容 | 出处 |
|---|---|---|
| 范围 | **四条纵切并行**：**W1 身份闭环** · **W2 判定闭环** · **W3 答疑闭环** · **W4 知识闭环** —— ★ 切法是"**域 × 纵深切片**"，**每个包是一条能演示的闭环**（不是横切"先把所有 model 写完"） | `docs/06 §11` 表头 · §11.2 |
| 出口 | **`docs/06 §4` 的第 1 / 2 / 4 / 11 / 12 项**（★ **2026-09-24 补第 12 项**：F6 · F15 · F16 三条 B 级 MUST 此前没有出口条件承载） | `docs/06 §11.2` |
| 顺序前提 | **W0 已完成**（阶段门 B 入口条件 2「骨架就绪」）；包间前置见第五章 | `docs/06 §11` 的"前置"列 |
| 与学校节点的对接 | 中期检查要求"**判定闭环端到端可演示**"（**W2 双绿**）—— ★ `docs/06 §13` 的那一行有**四项**条件，**本文只列与本轮强相关的一条** | `docs/06 §13` |
| ★ 谁核、核什么 | **`docs/06 §14.5` 的八条**（端到端走一遍并刷新页面 · 缺环演示 · A/B 可达演示 · 资产下界 · 写作用域 · 偏差回写 · ★ **三条 B 级 MUST 的断言面**〔F15 · F16 · F6〕） | `docs/06 §14.5` |

---

## 二、四个包 → 三个人（+ 域1 的伴随工作）

**一个人 = 一个域 = 一个包主域 = 一段别人不许直接编辑的路径**（`docs/05 §2.5` · `§3`）。★ 表格里的**代号**由小组在**仓外**对应到姓名。

| 包（工作包） | 领活的人 | ★ 包主（`docs/05 §2.5`） | 参与域 | 前置 | ★ 这个包"给别人什么"（交付物清单，原文见 `docs/06 §11`） |
|---|---|---|---|
| **W1 身份闭环** | **人二**（后端与数据） | **后端与数据** | 架构与集成（`#/login` 与 `core/auth.js`） | **W0** | 登录 → 令牌 → `GET /api/me` → 越权 404 → `must_change_pw` 与两把锁 · `DELETE /api/me/data` 的**四类处置** · `#/login` · ★ **教师侧 `#/admin` 与 5 个 `/api/admin/*` 端点** · ★ **`scripts/manage_users.py`**（**CLI 建管理员** —— F15：**接口不可创建管理员**；★ 2026-09-24 补认领） |
| **W2 判定闭环** | **人四**（前端页面与内容资产） | ★ **前端页面与内容资产** | 后端与数据（**引擎**）· 架构与集成（**外壳**） | **W0 · W1** | `GET /api/problems` · `POST /api/practice/grade`（**确定性** · `to_thread`）· `POST /api/practice/hint`（**提示阶梯**）· attempt **落库读回** · `GET /api/me/wrong` · `GET /api/me/progress` · **题库与错因库达下界**（`docs/06 §7`）· `#/practice` · `#/wrong` · `#/home` |
| **W3 答疑闭环** | **人三**（编排与工具） | **编排与工具** | 后端与数据（落库与 `egress` · **run 快照端点**）· 架构与集成（**只供外壳**；★ `#/settings` **页归域2** —— `docs/05 §4`）· ★ 前端页面与内容资产（**演示面 = `#/practice`** —— `docs/06 §14.2` 的第三条硬前置）· ★ **域1 那半份三件**（**2026-09-24 登记**：`core/api.stream()` 的 SSE 接收器 · **N15 首告 banner**（外壳级）· 别域页面的 `index.html` 挂载行 —— 见 `docs/06 §14.3` 卡 W3） | **W0 · W1** | `POST /api/chat` 的 **14 类事件**与 SSE 帧 · ★ **`GET /api/runs/{id}` 的 run 快照**（**F2 的第二处落点**）· 验证报告 + `must_render` · `qa_log.egress` · `GET` / `POST /api/me/notice` · **端点信任边界与 `X-Model-Key`** · `#/settings` |
| **W4 知识闭环** | **人三**（编排与工具） | **编排与工具** | 后端与数据（DAG）· 前端页面与内容资产（题型库） | **W0 · W2 的 `hint` 形状**（★ 题库格式已由 W0d 落地） | **余下三类资产达下界**（`docs/06 §7`）· `GET /api/knowledge/tree` / `formula` · F17 的拆解**走 `hint`** · F9 的**前端 BFS** · `#/knowledge` |

★ **三条必须知道的调度事实**（都来自 `docs/06 §11` 与 `docs/05`，不是本文的推论）：

1. ★ **W2 的包主刻意是域4**（理由在 `docs/05 §2.5`：W2 的演示面就是 `practice` / `wrong` / `home` 三页 + 题库 ⇒ **由最靠近"用户看到什么"的域来催**），而**引擎仍在域2** —— 这是"包主 ≠ 全包都自己写"的实例（`docs/05 §2.5`：**包主只有催 · 集成 · 演示 · 判定四件事，无权改别域目录**）。
2. ★ **人三同时领 W3 与 W4** ⇒ 人三**不是两条并行，而是串行 W3 → W4**（且 W4 还要等 W2 的 `hint` 形状 —— ★ 题库格式已由 W0d 落地）。
3. ★ **写入面是一条关键路径（评审 P1-5）**：`docs/05 §3` 把 `backend/app/api/**` 的**唯一写入域**判给**域2** ⇒ **除 W0 的 `/api/health` 外，其余 path 的接线（薄路由 · 鉴权 · 序列化）都在域2 一人身上**（业务判定在 `domain/`、编排在 `graph/`、验证手段在 `tools/`，按域分给域2 / 域3）⇒ **"四条并行"在写入面上是"一条关键路径（域2）+ 一条串行链（域3 的 W3→W4）+ 域4 的三页"**。逐包的"写入域"列见 `docs/06 §14.8`；`docs/05 §2.5` 的负载明账原文写着"**域2 结构性最重**"。★ **跨域的步骤必须按 `docs/05 §3` 规则 1 记账**（如 W1 ④ 的 `#/login` 归域1、W3 ⑥ 的 `#/settings` 页归域2）。
4. ★ **批次 2 期间域1（架构与集成）的门禁工作不能停**：四条纵切里有两条（W3 的 `events` · W4 的 `hint`）要用到契约，而**契约的唯一审批人是 R1 = 架构与集成域**（`docs/04 §6`）；它同时是 `content_rev` 复算的通道。★ 另：**阶段门 B 的第 3 / 5 / 7 / 8 / 9 / 10 项**的责任人都含域1（★ 第 3 项的原文是"**架构与集成（门禁）· 各域（被查项）**"，不只域1 —— **2026-09-24 补**：原枚举漏了**第 9 项**〔运维演练 ②③④：备份调度 · 恢复演练首次 · 压测〕，而它的责任人也写着"架构与集成"，见 `docs/06 §11` 的 W5 行与 §4）。

---

## 三、完整任务（逐包、逐步，每步写出"做完应当呈现成什么样"）

> ★ **读法**：每张表的"要做什么"逐条对应 `docs/06 §11` 的**交付物清单**；"做完应当呈现成什么样"一律给**判据指针**（`docs/06 §4` 的项号 · `docs/01 §6` 的达标线 · 需求 ID · `docs/06 §7` 的下界），**本文不复制判据全文**（`docs/06 §11` 的纪律：判据一律用指针）。
> ★ 第二轮的**任务卡在 `docs/06 §14.3`**（交付 · 闭合 · **逐卡"不做什么"**）；下表的粒度是从 `§11` 的交付物逐条展开，每条的判据来自 `§11` 的"闭合"列与 `§14.3` 同一份指针。

### W1 · 身份闭环（人二 · 包主 后端与数据）

| 步 | 要做什么（`docs/06 §11` 的交付物） | ★ 做完应当呈现成什么样（判据） |
|---|---|---|
| ⓪ | ★ **契约形状已就位（原为"前置"，2026-09-24 落地）**：`GET /api/admin/audit` 的**审计行形状**此前不在任何契约里（`docs/04 §9` 缺口 9）⇒ 现已在 **C3 `v1.3`** 落地：`definitions.auditRow`（`user.schema.json`） | ★ **本卡不再有"先走提案 → R1 审批"的启动成本**；**只剩一项待确认**：`auditRow.action` 的 **8 个取值是起草值**（契约里逐项标了对应端点；不含 `export_students`）—— 由本卡确认，或按 `docs/04 §6` 再提案（新增枚举值属兼容变更） |
| ① | ★ **登录 → 令牌 → `GET /api/me`** 的最小闭环（`docs/02 §6.1` 的身份组）—— ★ **本步先发布**（W2 / W3 的前置只到这里，见 `docs/06 §14.3` 的内序） | `docs/06 §4` 第 1 项的**身份侧**：端到端走一遍**并刷新页面**；契约 `docs/04 §3` 的身份 path 全覆盖 |
| ② | **越权 404** 与 **`must_change_pw` 两把锁** | `docs/01 §6` 的达标线 **F15 越权隔离**（学生 A 读不到 B 的任何数据，含聚合与导出）· `docs/02 §6.3` 的 `must_change_pw` 是 **403 且强制跳设置页、不清令牌** |
| ③ | **`DELETE /api/me/data` 的四类处置** | `docs/01 §6` 的达标线 **F16 删除读回**（四表硬删读回 0 行 · `student_token` 级联删 · `student_user` 脱敏 · `student_audit` 保留） |
| ④ | **`#/login`**（外壳侧：架构与集成） | 页面可开、路由可寻址（`docs/02 §7.1`）；`core/auth.js` 的三个键名不动（`docs/02 §7.4`） |
| ⑤ | ★ **教师侧 `#/admin` + 5 个 `/api/admin/*` 端点** + ★ **`scripts/manage_users.py`**（**CLI 建管理员** —— 它是 `#/admin` 与"第一个管理员账号"的**前件**）—— ★ **内序**：可排在 **W2 双绿之后**（中期检查的硬节点是 W2 双绿） | ★ `docs/06 §11` 的 W1 行已把它写成**必须**（F15 是 MUST、达标线含"管理员可见本机范围内全体学生数据"）；页面责任域见 `docs/05 §4`（`#/admin` = 后端与数据）；★ **建账号的判据 = `docs/03 §8.4` 第 7 步**（F15：**接口不可创建管理员**） |
| ⑥ | **R-J / R-M / R-O 的凭据纪律**（写入点） | `docs/06 §4` 第 3 项的**凭据写入点**；`docs/02 §1.6` 的 R-J（凭据只走请求头）· R-M（七面不落）· R-O（日志只记 ID 与动作） |

### W2 · 判定闭环（人四 · 包主 前端页面与内容资产；引擎在域2）

| 步 | 要做什么 | ★ 做完应当呈现成什么样（判据） |
|---|---|---|
| ① | **`POST /api/practice/grade`**：确定性、`to_thread` | `docs/01 §6` **判定准确率**（`answer_kind=value` 的题**全部**由确定性代码判定；统计范围 = `gradable=true`）· **判定延迟**（纯计算题端到端**有上界**、并按该行的取样口径取值 —— **数值与取样方式见 `docs/01 §6`**）· 判定链路**无 LLM 调用** |
| ② | **`POST /api/practice/hint`**：提示阶梯 | `docs/01 §6` **提示阶梯生效**（未提交前**不返回**完整解析；每次提示 `hint_used` 精确 +1） |
| ③ | **attempt 落库并读回** + `GET /api/me/wrong` / `progress` | ★ **本卡的闭合项以 `docs/06 §11` 的 W2 行为准**：`§2.1` 的 **F4 · F5 · F6 · F7 · F8 · F19**（含 **F8 能跨会话读到 F7 的数据**）· ★ **端到端走一遍并刷新页面**（只写不读不算通 —— `docs/06 §4` 第 1 项）· ★ **F5 的两层限定（评审 P0-4）**：**功能闭环**（错因库 + 判定链）**在本轮**；其**正式集盲测**的门是 **C**（`docs/06 §2.1`），且正式集**不入仓、由非实现者执行**（`docs/06 §5` 第 2 项）⇒ **不在本轮**。★ **注意（2026-09-24 改正）**：`docs/06 §4` 第 1 项那一行**还含 F2（逐步解题）**，而 **F2 属答疑链 ⇒ 归 W3**（`docs/01 §4.1`）—— 原判据把整行照抄过来，会让人以为 W2 能闭合 F2 |
| ④ | **错因库达下界**（★ **题库与题型库已由 W0d 达标**，本轮不再计入） | `docs/06 §7`：`content/mistakes.json` **达下界**（每条含 `category` / `kc_ids`）—— ★ **主责域 = 域2**（`docs/05 §2.2`）；下界数值一律见 `docs/06 §7`（★ 复核实测：`problems` 181 · `types` 102 均已过线）—— **两条的下界与长期目标一律见 `docs/06 §7`**；★ **机器化校验归 W5**，本轮按卡 4 的"**等价的手跑校验**"办 |
| ⑤ | **三页：`#/practice` · `#/wrong` · `#/home`** | 页面责任域见 `docs/05 §4`；`#/home` 已有静态首屏（`#8`），本卡把它接上真实数据（**只写不读不算通**） |
| ⑥ | **F11 的降级话术**（薄弱点不可用时的统一文案） | `docs/01 §6` **复习有效性**行：样本不足时必须显示统一文案"**数据不足，暂不显示薄弱点**"，**不设百分比门禁**（`docs/06 §9` ⑤）· `docs/06 §4` 第 2 项 |
| ⑦ | ★ **回归基线的测量条件**（首次全量测量值） | `docs/06 §4` 第 6 项：首次全量测量值写入 `docs/01 §6` 的"**解题正确率**"格并**冻结**（只许上调）；★ **判定准确率行是硬地板，不是回归基线** |

### W3 · 答疑闭环（人三 · 包主 编排与工具）

| 步 | 要做什么 | ★ 做完应当呈现成什么样（判据） |
|---|---|---|
| ⓪ | ★ **契约形状已就位（原为"前置"，2026-09-24 落地）**：`GET /api/runs/{id}` 的 **run 快照形状**此前不在任何契约里（`docs/04 §9` 缺口 8）⇒ 现已在 **C4 `v1.2`** 落地：`definitions.runSnapshot` + `definitions.solutionStep`（`events.schema.json`） | ★ **本卡不再有"先走提案 → R1 审批"的启动成本**；**只剩一项待确认**：`runSnapshot.status` 的 4 个取值是起草值 —— 由本卡确认，或按 `docs/04 §6` 再提案。★ **并且契约写死了一条**：`status: done` 且属 solve 类解答时，本端点的 `steps[]` 必须与该次解答事件的 `payload.steps` **逐值相同**（同一次生成、同一份数据）—— 这就是 F2「两处落点」的可断言形式 |
| ① | **`POST /api/chat` 的 14 类事件与 SSE 帧** · ★ **`GET /api/runs/{id}` 的 run 快照**（F2 的**第二处落点**，形状见 `contracts/events.schema.json` 的 `definitions.runSnapshot`） | `contracts/events.schema.json` 的 **14 类**（`docs/04 §7` 校验⑤）· `docs/02 §4.2` 的"断流即重新发起、不做重放缓冲"· `docs/02 §4.2-7` 的 `steps: [{说明, 式子}]` |
| ② | **验证报告 + `must_render`** | `docs/01 §6` **诚实性**（F3 · N2）：一致率要求是**全量断言**（不是抽查）—— `level ≤ 手段允许的上限` 且 `conf ≤ cap(level)`；**数值见该行** |
| ③ | ★ **`sympy_recompute` 与 `symbolic_equivalence` 两个手段** | `docs/06 §4` **第 11 项**：不实现这两个手段则**验证级别恒为 D**；★ 与 `docs/01 §6` **错误拦截率**（N1）的地板直接耦合 —— 注入集里**须有一部分道次落在 A/B 级可达类别上**（**比例见 `docs/01 §6` 的 N1 行**；★ 该数值地板属**门 C** —— `docs/06 §2.1`，**本轮只登记耦合**），且拦截方式必须是**重算不一致** |
| ④ | **`qa_log.egress`** + **`GET` / `POST /api/me/notice`** | `docs/02 §1.5` 的出网面口径（`egress` 是判据，不是部署形态）· N15 / N16 的告知与去向 |
| ⑤ | **端点信任边界与 `X-Model-Key`** | `docs/02 §6.3` 的模型类 `code`（`model_key_missing` / `model_key_invalid` / `model_unreachable` / `model_quota_exceeded` 三处一致）· `contracts/chat.schema.json` 的 `x-invariants`（凭据**不得**出现在 body / query） |
| ⑥ | **`#/settings`** —— ★ **页归域2**（`docs/05 §4`）；域1 **只供外壳**（`core/auth.js` / `core/api.js`），**2026-09-24 改正**：原括注写"外壳侧：架构与集成"，与 `docs/06 §11` / §14.3 打架 | `docs/02 §7.1`（`#/settings?tab=model` · `#/settings?tab=notice` 可寻址）· `docs/02 §7.4`（`dsh.model_key` · `dsh.endpoint` 的键名） |
| ⑦ | ★ **参与域那半份（域1 · 2026-09-24 登记）**：① **`core/api.stream()`** —— 拆 SSE 帧 · `seq` 去重 · **终帧语义** ② **N15 首告 banner**（**外壳级**）③ **别域页面的挂载行**（`frontend/index.html`：加 `import` + 删 `PAGES` 占位） | ① 判据 = `docs/02 §6.1`（`POST + fetch 流`，非 `EventSource`）· §6.5-2（**终帧必须显式**）· §4.2-1（`seq` **只去重**）；线格式的唯一权威 = `contracts/events.schema.json` 的 `x-frame-conventions` ② 文案取 `noticeStatus.message`（标 **`must_render`** ⇒ R-I），确认走 `POST /api/me/notice` `{ack:true}` ③ `docs/03 §7④` 第 3 步（后注册者生效） |

### W4 · 知识闭环（人三 · 包主 编排与工具）

| 步 | 要做什么 | ★ 做完应当呈现成什么样（判据） |
|---|---|---|
| ① | **本轮实际剩下的三类资产**：知识点 **DAG**（域2）· **公式库**（域3）· 错因库（域2）—— ★ **本卡只对 DAG 与公式库**；**错因库属域2，本卡只做"对账"、不认领写入**（`docs/05 §2.2` · `§3`；跨域必须记账） | `docs/06 §7`：`dag.json` **规模达下界**（**无环** + `depends_on` 单向）· `formula.json` **条数达下界**（**名称唯一**，重名 ⇒ 启动报错退出）—— **两条的下界数值见 `docs/06 §7`**；主责域见 `docs/05 §2.2` |
| ② | **`GET /api/knowledge/tree` / `formula`** | `docs/04 §3` 的 path 覆盖（第 ④ 项校验）· 契约 `knowledge.schema.json` |
| ③ | **F17 的拆解走 `hint`** | `docs/04 §9 缺口 4` 的落点（F17 的拆解形状随 `hint` / `chat` 落） |
| ④ | **F9 的前端 BFS（上溯）** | `docs/04 §9 缺口 5` 的裁定："**F9 的上溯由前端自己做**"；`#/knowledge` 可寻址（`docs/02 §7.1`） |
| ⑤ | **`#/knowledge`**（脚本/前端侧：域3 与域4 协作） | 页面责任域见 `docs/05 §4`（`#/knowledge` = 编排与工具） |
| ⑥ | **`content_rev` 可复算的前提**（五类齐备 + `MANIFEST.json`） | ★ **前提 = `docs/06 §4` 第 4 项**（五类齐备）；**复算一致本身（第 5 项）归批次 3**（`docs/06 §11.2`）—— 门禁用 `docs/02 §3.3` 规则 2 的算法**重算**并与 `MANIFEST.json` 内的值比对（`docs/04 §7` 校验⑥）；★ **`MANIFEST.json` 的归属是架构与集成**（`docs/05 §3`），**不是资产作者**（2026-09-24 改：原写"判据 = 第 5 项"，与批次表打架） |

---

## 四、本轮结束时，整体应当呈现的形态

★ **"这一轮做完了" = `docs/06 §4` 的这五项能当场演示**（`docs/06 §11.2` 给批次 2 的出口就是这五项）：

| # | 出口条件（原文见 `docs/06 §4`） | 谁能当场演示 |
|---|---|---|
| **1** | **三条主链路的 MUST 闭环全通** —— `docs/01 §6` 的闭环行 **F2 · F4 · F5 · F7 · F8 · F19 全通**，且**端到端走一遍并刷新页面**（只写不读不算通） | 后端与数据 · 编排与工具 · 前端页面与内容资产 |
| **2** | **SHOULD 环节缺环时显式降级** —— F9 / F10 / F11 / F17 / F18 若缺失，界面必须显示**统一降级话术**，**不得静默** | 前端页面与内容资产 |
| **4** | ★ **五类内容资产达下界**（`docs/06 §7` 的五条） | `docs/06 §7` 的五个主责域 |
| **11** | ★ **验证级别 A/B 可达** —— 实现 `sympy_recompute` 与 `symbolic_equivalence` | 编排与工具 |
| **12** | ★ **身份链与提示阶梯的三条 B 级 MUST 各自可断言**（**2026-09-24 补**）—— **F15** 越权用例 · **F16** 逐表读回 0 行 · **F6** 未提交前不返回完整解析且 `hint_used` 精确 +1（三行判据见 `docs/01 §6`） | 后端与数据 · 前端页面与内容资产 |

★ **另外三条与本轮强相关、但判据在别处**：

- **`docs/06 §4` 第 3 项（R-A…R-Q 门禁绿）· 第 5 项（`content_rev` 可复算）· 第 7 / 8 / 9 / 10 项（单 worker / 启动校验 / 运维演练 ②③④ / CI 生效）** 的责任域**都含**架构与集成（★ **第 3 项的原文是"架构与集成（门禁）· 各域（被查项）"**，不只域1 —— `docs/06 §4` 责任人列；★ **2026-09-24 补第 9 项**：原枚举漏了它）⇒ **它们在批次 2 期间不能停**，但也**不是批次 2 的出口**（属 `§11.2` 批次 3）。
- **`docs/06 §4` 第 6 项（回归基线录入并冻结）** 的测量条件由 W2 提供（见第三章 W2 第 ⑦ 步）；★ **录入与冻结在批次 3**（`docs/06 §11.2`）。
- ★ **本轮"没有门"的三条（评审 P2-16）**：**依赖方向扫描 · `api/` 薄（`docs/03 §3.2`）· R-D / R-E / R-H / R-I** 都不在 `scripts/verify.sh` v0 的范围（`docs/06 §12.5` ⑥ 已如实登记）⇒ 本轮**靠 review**（`docs/05 §7.2` 的"合并前必须做完的三件事"），**别把"门禁绿"读成本轮的结构性纪律已合规**。

★ **检查点（谁核、核什么）**：见 **`docs/06 §14.5`** 的八条 —— ① 各自走 `docs/03 §8.4` 第 1–5 步 · ② **端到端走一遍并刷新页面**（三条主链路）· ③ **缺环演示**（统一降级话术）· ④ **A/B 可达演示**（重算不一致的拦截）· ⑤ 五类资产达下界 + `content_rev` 复算的前提 · ⑥ 写作用域核查 · ⑦ 实测偏差回写 · ⑧ ★ **三条 B 级 MUST 的断言面**（F15 · F16 · F6）。

---

## 五、卡间依赖与接口发布顺序

**关键路径**（`docs/06 §11` 的原图，原文照录）：

```
W0 ──┬─▶ W1 ──┬─▶ W2 ──▶ W4 ──┐
     │        └─▶ W3 ──────────┼─▶ W5 ──▶ 阶段门 B
     └─（W0 期间：另外三个域**不空等**，见 docs/06 §11.1）
```

**★ 本轮的接口发布顺序**（据 `docs/06 §11` 的"前置"与"交付物"两列整理；**谁在等**取自 `docs/06 §14.2`（本轮）与 `§11` 的"前置"列 —— ★ **2026-09-24 改正**：原写"取自 `§11.1` · §12.2"，而 §11.1 是 **W0 并排自立**、§12.2 是**第一轮**的边图）：

| 序 | 谁发布什么 | 谁在等 | 出处 |
|---|---|---|---|
| 零 | ★ **人二（域2）**：**幂等建表入口 + 七张 `student_*` 表 + `identity/` / `learning/` 存储层**（评审 P1-6 —— 此前**没有任何交付物承载**，而 W1 / W2 / W3 都要写库） | **W1 · W2 · W3**（它们的读写都落在这七张表上） | `docs/06 §14.2` 的首发项 · `docs/02 §3.1`（七张表）· **§3.6**（建表与事务纪律 —— 2026-09-24 改正：原写 §3.2 / §8，两处都不是它们） |
| 一 | **人二（域2）**：W1 的**令牌与 `GET /api/me`**（身份最小闭环） | **人三**（W3 要登录态才能测 `chat` 与 `notice`）· **人四**（W2 的 `#/home` 要"我的数据"） | `docs/06 §11` 的 W2/W3 前置 = W1 |
| 二 | ★ **已完成（W0d）**：题库与题型库**已于 `#7` 落地**（`content/problems.json` 181 · `types.json` 102，契约 0 errors）⇒ 原"W4 等题库格式"的前置**已满足** | **无人再等它**；W4 真正等的是序三的 **`hint` 形状** | `docs/06 §11` 的 W4 前置（2026-09-21 已改指 `hint`） |
| 三 | **人二（域2）**：W2 的**判定引擎入口**（`grade` / `hint` 的返回形状） | **人四**（页面）· **人三**（W4 的 F17 拆解**走 `hint`**） | `docs/06 §11` 的 W2/W4 交付物 |
| 四 | **人三（域3）**：W3 的**事件帧形状**（14 类事件的 payload）· ★ **`GET /api/runs/{id}` 的 run 快照**（形状已在 **C4 `v1.2`** —— 见 ⓪） | **人四**（`#/practice` 的事件渲染 —— ★ 本轮**没有独立答疑页**；`#/trace` 是 **MAY · 未排期**，不在本轮）· **批次 3 的 W5**（前端冒烟） | `docs/06 §11` 的 W3 交付物 · `docs/02 §1.6` 的 R-I · `docs/06 §14.2` 的第三条硬前置 |
| 五 | **人一（域1）**：**契约审批与 `content_rev` 通道**（R1）· **门禁的全量扩展**（W5 才收口） | **全体**（★ **两条缺口已在起飞前落地**：缺口 9 = C3 `v1.3` 的 `auditRow` · 缺口 8 = C4 `v1.2` 的 `runSnapshot`，2026-09-24 —— 见 `docs/04 §9`；**只剩两处枚举值待 W1 / W3 确认**） | `docs/06 §11.2` 的注 · `docs/04 §6` |

**三条接口纪律**（`docs/06 §12` 的四条边 + `docs/05 §3` 规则 1 的原文口径）：**① 接口一旦发布即冻结**（改动走 `docs/05 §3` 规则 1 记账，分歧走 `docs/05 §7.1`）· **② 模块之间只经接口交换**（直接改别人的文件算越界）· **③ 契约的语义 / 破坏性变更只有一个审批人 = R1 = 架构与集成**（`docs/04 §6`）。

---

## 六、边界：这一轮**不做**什么

★ **逐卡的"不做什么"在 `docs/06 §14.3`**（每条都给了依据，`§12.3` 那一份只覆盖第一轮）。本章再补**跨卡的三类边界**（都能机械判定）：

| 边界 | 内容（可机械判定） | 权威 |
|---|---|---|
| **写作用域** | 每个包的代码路径只由**一个**域写；跨域改动要"**经 `<域>` 同意**"（写在提交信息正文里） | `docs/05 §3` 及其规则 1 |
| **依赖方向** | `domain/` 只依赖 `core/` · `tools/` 与 `learning/` 不 import `api/` / `graph/`（`learning/` 只 import `core/`+`domain/`）· `identity/` 不依赖 `domain/` · `graph/` 不 import `api/` · `api/` 不得被下层 import · 前端 `features/*` 不互相 import、`components/` 不 import `features/`、前端禁用一切外部 CDN / npm 包 | `docs/03 §3.1` 的强制表（每条都有检查方式） |
| **结构与口径** | `api/` 单路由函数 **≤ 40 行**、不含业务分支、不写 SQL · 业务逻辑**不得写进图节点** · 同步 CPU 工作一律 `to_thread` | `docs/03 §3.2` 三条结构性纪律 |
| **不许"没报错就算过"** | 删除要**逐表读回 0 行** · 写库要**测试读回** · 示例要**能通过自己的校验** · 计数要从表体算出来 | `docs/01` 承诺 9 · `docs/04 §4` · `README` 的"五条硬约束"第 5 条 |
| ★ **本轮口径（全轮生效）** | ① **N5 是条件 MUST**（探针后置期间不生效，不得计为已完成）· ② **MUST 满足率上限不是 100%**（F1 的注入式读屏与 F10 的 PDF 溯源属形态后置，明账）· ③ **禁止用主站的满足度抵充探针的缺失** · ④ **禁止需求 ID 区间记法**（写 ID 列表，不写"F1→F8 全通"）· ⑤ **观察指标不设门禁** | `docs/06 §9` 五条口径 |

---

## 七、本轮负载明账（★ 建议表，不是口径）

★ **依据**：`docs/06 §11` 的交付物清单 + `docs/05 §2.5` 的包主/参与域 + `docs/06 §14.8` 的人手视图。★ **本节不含"交付物条数"这类自造计数** —— `docs/06 §11` 里没有这个数（本稿初版自数过一列，因与实际不符已删）；表里只留**有权威的三样**：**领活的人（= `§11.2` 的包主）** / 参与域 / 备注 —— ★ 四条纵切里**包主都等于领活的人**（参与域是协作方）。**"谁的活更重"的判断属小组**，本文不代排。

| 包 | 领活的人 | 参与域 | 备注 |
|---|---|---|---|
| **W1 身份闭环** | 人二 | 1 + 1（域1 供 `#/login`） | ★ `#/admin` 是 2026-09-17 才补进交付清单的（`docs/06 §11` 记了它的由来），**别漏** |
| **W2 判定闭环** | 人四（**包主**） | 2（域2 引擎 · 域1 外壳） | ★ **包主在域4、引擎在域2** ⇒ 它是最需要"催 + 集成"的一个包 |
| **W3 答疑闭环** | 人三 | 3（域2 落库/`egress`/run 快照 · **域4 演示面** · 域1 外壳） | ★ **第 11 项（A/B 可达）是这一轮唯一"不做就构造不可能"的项** · ★ **`#/settings` 页归域2**（域1 只供外壳 —— `docs/05 §4`）· ★ **事件的上屏在域4 的 `#/practice`**（本轮没有独立答疑页 —— `docs/06 §14.2`） |
| **W4 知识闭环** | 人三 | 2（域2 DAG · 域4 题型库） | ★ 人三**串行**做 W3 → W4；且 W4 等 **W2 的 `hint` 形状**（★ **2026-09-24 改正**：原写"题库格式"，而题库格式已由 W0d 落地；`docs/06 §11` 与 §14.2 都已改指 `hint`） |
| **（域1 的伴随工作）** | 人一 | —（域内） | ★ `docs/06 §11.2` 明写"**批次 2 期间域1 的门禁工作不能停**"；内容：契约审批（R1）· 门禁与 CI · `#/login`/`#/settings` 的外壳侧 · `content_rev` 通道；人手对应另见 `docs/06 §14.8` |

★ **三条调度建议（★ 建议，不是口径 —— 与 `docs/第一轮分工.md` §七 同形；只给顺序，仍不代排"谁的活更重"）**：

1. ★ **不要再往人二的队列里加东西** —— 他是本轮**唯一的关键路径**（`docs/06 §14.8`）：**W1 的最小身份闭环 + 首发建表项**必须先发布，其余按卡内序压后（`#/admin` 一批已明写可排在 W2 双绿之后）。
2. ★ **人三在等 W1 的这段等待期，用"不等人的东西"填满**：`content/formula.json`（**域3 自己的资产**）· 事件帧的 **14 类 payload 冻结** · ★ **确认 `runSnapshot.status` 的 4 个取值**（契约已在 **C4 `v1.2`**、标着"待域3 确认"——`docs/04 §9` 缺口 8）—— 三件都不依赖 W1，也不依赖建表。
3. ★ **人四先做对账与页面骨架，别把三页排到最后** —— 两类资产（`problems` 181 · `types` 102）已由 W0d 交付；而 **`#/practice` 是 W3 的演示面**（`docs/06 §14.2` 的**第三条硬前置**）。

---

## 八、共享约定

**第一轮那张表继续生效**（`docs/06 §12.1` 的原话："第一轮就要守住，**之后不再重复**"）：

| 约定 | 值 | 出处 |
|---|---|---|
| 起服务 · 打开前端 · 跑门禁 · 跑用例 | `docs/03 §8.4` 的**第 1–5 步** | `docs/03 §8.4` |
| Python / 依赖 | **3.11+** · `requirements.txt` / `requirements-dev.txt`（**已钉精确版本**） | `docs/03 §8.4` |
| **单 worker** | 启动时检测，**>1 拒绝启动并说明原因** | `docs/02 §8-15` |
| ★ **同源** | 前端由**同一个服务**托管 —— **不另开静态服务器、不配 CORS** | `docs/03 §8.1` · §8.4 |
| 依赖方向 | `domain/` **只依赖 `core/`**；`api/` 不写 SQL、**单函数行数有上界** | `docs/03 §3.1` · §3.2 |
| 测试命名 | 测试文件与被测模块**同名**（入口与配置归架构与集成） | `docs/05 §3` |
| 假数据 | 契约的 **`examples`** 就是前端的假数据源 | `docs/04 §4` |
| 注册表 | **R-Q ①**：允许集合**非空且非通配** | `docs/02 §1.6` |
| 代码怎么落到 `main` | 分支名 · 提交信息 · **squash 合并** · 合并前三件事 · 冲突归谁解 · 行尾 | `docs/05 §7.2` |
| 新增一个页面 / 节点 / 工具 / 契约 | **四个扩展点各有一张现成的清单** | `docs/03 §7` ①–④ |

**第二轮新增 / 特别提醒五条**（都有权威，不是本文新定 —— ★ **2026-09-24 改正计数**：此前写"三条"而表体已列 4 条）：

1. ★ **契约的语义或破坏性变更只有一个审批人 = R1 = 架构与集成**（`docs/04 §6`）；W3 的 `events`、W4 的 `hint` 都会碰契约 ⇒ **走提案，不顺手改**（`docs/06 §12.6` 规则 2 的口径对本轮同样成立）。
2. ★ **`content/MANIFEST.json` 与 `content_rev` 归架构与集成**（`docs/05 §3`：它是**派生文件**）⇒ 资产作者只交**自己那几类** `content/*.json`，**不要各写一份 MANIFEST**。
3. ★ **包主不放权**：包主只做**催 · 集成 · 演示 · 判定"这个包够不够了"**四件事，**无权直接改别域的目录**（`docs/05 §2.5` 原文）。
4. ★ **代理域已登记**（`docs/06 §11` 的包表**新增"代理域"列**，2026-09-21 确认）：包主缺席时由代理域维持"催 · 集成 · 演示 · 判定够不够"，**写作用域互斥不变**（代理域**仍不能**直接改别域的目录）；职责与减员降级顺序一律见 `docs/05 §2.5`。
5. ★ **域2 是本轮唯一的关键路径**（`docs/06 §14.8` 的"写入域"列 + `docs/05 §2.5` 的负载明账）：**四条纵切在代码面上共用一个人** ⇒ **两个支援窗口减不了它**（它们的代码面都不在关键路径上）。两条处置见 `docs/06 §14.8`：**① 写明关键路径**（W1 的**最小身份闭环 + 首发建表项先发布**）· **② 允许按 `docs/05 §3` 规则 1 逐文件临时授权**（提交信息正文写明"经 `后端与数据` 同意"；★ **授权是逐文件、逐次的，不改写作用域表**）。★ **把 `api/**` 按模块拆给各域不在本轮** —— 留到批次 2 的中期复盘。

★ **开工前自检（每人五条 —— 判据全部在别处，本表只留指针；跑完各自在自己机器上留档，即 `docs/06 §14.5` 检查点第 1 条）**：

| # | 自检 | 判据 / 去哪儿看 |
|---|---|---|
| 1 | ★ **我能推一个 `<域>/<主题>` 分支吗？** | 判据是"**推得上去**"，**不是"能 clone"**（public 仓没被邀请也能 clone）—— `docs/05 §7.2` 首次托管**第 4 步**（★ 截至 2026-09-24 实测：**域1 / 域3 / 域4 已走通，域2 待验证**） |
| 2 | ★ **我的提交计在自己名下吗？** | `git log` 的作者**显示我的 GitHub 账号**，`user.email` 用 **noreply** 地址 —— 同节**第 5 步** |
| 3 | ★ **本机跑通 `docs/03 §8.4` 第 1–5 步了吗？** | `python3 -m venv .venv` → 装两个 `requirements`（★ 建前先 `git check-ignore .venv`）→ 起服务 → `bash scripts/verify.sh` → `python -m pytest -q backend/tests`。★ **要用 venv 的 python**：系统 python 缺 `jsonschema` 时门禁会 **9/10** |
| 4 | ★ **我知道"做完长什么样"吗？** | `docs/06 §11` 我自己那一行（交付清单）+ `§14.3` 我那张卡（交付 · 前置 · **不做什么**）+ `§14.5` **八条**检查点 |
| 5 | ★ **我知道动别人的目录要记账吗？** | `docs/05 §3` 规则 1：提交信息正文写"**经 `<域>` 同意**"（★ 域2 的关键路径还可按 `docs/06 §14.8` 的**逐文件临时授权**办） |

---

> ★ **再次声明**：本文是**投影**。任务、判据、边界的唯一权威是 `docs/06`（配合 `docs/03 §8.4` 与 `docs/05`）；本文若与它们不一致，**以它们为准**，并请把差异回写（`docs/06 §12.5` 检查点第五条的做法：**改文档，不靠人记**）。
