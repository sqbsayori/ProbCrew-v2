# 运行时依赖（服务进程用）—— 卡 1 · W0a 第 ① 步
#
# 包清单的权威来源：docs/03 §8.4 第 1 步（"首次取值（W0 定稿）"）。
# 版本口径：★ 钉精确版本（W0 对 docs/03 §8.4 三个待定问题之①的回答）。
#   理由：ADR-0003 把"验证 LangGraph 在本地 Python 上可装可用"排为阶段 B 的第一个动作，
#   这份清单就是那条动作的产物；钉死版本才让"可装可用"这个结论可复现。
#   ★ 实测环境：Python 3.14.7（本机）+ 依赖闭包 49 个包全部解析并安装成功（2026-09-20）。
#
# 依赖方向提醒：本文件属"架构与集成"（docs/05 §3）。
bcrypt==5.0.0
fastapi==0.141.1
langgraph==1.2.11
pydantic==2.13.5
sympy==1.14.0
uvicorn==0.53.0
